import React from 'react';
import { useNavigate } from 'react-router-dom';
import Login from './Login';

const Register = () => {
    // Registration is handled in the Login page with tabs
    return <Login />;
};

export default Register;
